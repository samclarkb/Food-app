import { scanButton } from './ui.js'
import { handleRoutes } from './router.js'

// declaring the function that activates the camera and the function thats taking care of the hash route
scanButton()
handleRoutes()
